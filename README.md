# Use http-server with nodejs

    Move from `node-static` to `http-server` on npm

## Install dependency

    `npm i`

## Run server in `development` or `production` mode

    `npm run backend:dev` or `npm run backend:server`
